// 逻辑运算符和自增自减特性测试

int a = 10;
int b = 20;
int c = 30;

// 自增自减运算符
a++;
b--;
++c;
--a;

// 逻辑运算符
if (a > 5 && b < 30) {
    int x = 1;
}

if (a == 10 || b == 20) {
    int y = 2;
}

// 复杂逻辑表达式
while (a >= 0 && b <= 100 || c != 0) {
    a--;
    b++;
}

// 组合使用
if ((a++ > 5) && (b-- < 25)) {
    int result = a + b;
}

// 嵌套逻辑
if (a > 0 || (b > 0 && c > 0)) {
    int flag = 1;
}

return 0;
